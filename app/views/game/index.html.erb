<table>
  <tr class="button-row">
    <td>
      <%= button_to("drop", chits_path(column_row: 1)) %>
    </td>
    <td>
      <%= button_to("drop", chits_path(column_row: 2)) %>
    </td>
    <td>
      <%= button_to("drop", chits_path(column_row: 3)) %>
    </td>
    <td>
      <%= button_to("drop", chits_path(column_row: 4)) %>
    </td>
    <td>
      <%= button_to("drop", chits_path(column_row: 5)) %>
    </td>
    <td>
      <%= button_to("drop", chits_path(column_row: 6)) %>
    </td>
    <td>
      <%= button_to("drop", chits_path(column_row: 7)) %>
    </td>
  </tr>

  <% @game_state.each do |row| %>
    <tr class="gameboard-row">
      <td><%= row[0]%></td>
      <td><%= row[1]%></td>
      <td><%= row[2]%></td>
      <td><%= row[3]%></td>
      <td><%= row[4]%></td>
      <td><%= row[5]%></td>
      <td><%= row[6]%></td>
    </tr>
  <% end %>

</table>
